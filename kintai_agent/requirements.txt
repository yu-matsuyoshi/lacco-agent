# Core dependencies for AgentCore Runtime (v2)
strands-agents[a2a]>=1.0.0

# AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# AWS Bedrock AgentCore SDK (with strands memory integration)
bedrock-agentcore[strands-agents]>=1.0.0

# MCP (Model Context Protocol) Client
mcp>=1.0.0

# Data processing
pandas>=2.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
