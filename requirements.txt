# ============================================
# Development dependencies (local environment)
# ============================================

# Core dependencies
strands-agents>=1.0.0

# AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# AWS Bedrock AgentCore SDK
bedrock-agentcore-sdk>=10.0.0

# MCP (Model Context Protocol) Client
mcp>=1.0.0

# Data processing
pandas>=2.0.0

# Web UI (local only - not deployed to AgentCore Runtime)
streamlit>=1.30.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Note: AWS CDK is managed via npm in the cdk/ directory (TypeScript)
# The Python CDK packages are NOT used in this project
